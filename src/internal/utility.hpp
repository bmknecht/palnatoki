
extern "C"
{
#include "../common/common.h"
}


#define RETHROW(expr)       \
{                           \
    int res = expr;         \
    if(res != PNT_SUCCESS)  \
        return res;         \
}

typedef int (*ssObjective)(float, float*);
typedef int (*ssObjective)(float, float*);
typedef int (*ssObjective)(float, float*);

typedef int (*ssObjective)(float, float*);
typedef int (*ssObjective)(float, float*);
typedef int (*ssObjective)(float, float*);
